{
  "name": "store-finder-root",
  "version": "1.0.0",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload",
    "seed": "cd backend && python data/seed_runner.py",
    "test:backend": "cd backend && python -m pytest tests/test_api.py -v"
  }
}
